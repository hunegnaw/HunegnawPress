/**
 * Standard vertical padding for full-width page block sections.
 *
 * Every block that renders as a top-level <section> uses this so the page
 * has consistent rhythm between blocks. Change it here to retune the whole
 * site at once. Text Section blocks may override via their `paddingY` prop.
 */
export const SECTION_PADDING_Y = "py-16";
